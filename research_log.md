# BTC Trend v1 — Research Log

## القرار النهائي الحالي
Baseline (`strategies/btc_trend_v1.py`) **مجمّد** — ممنوع أي تعديل بارامترات أو إضافة إنديكيتورات جديدة عليه حاليًا.
الخطوة القادمة: توسيع البيانات التاريخية (6-12 شهر BTC-USD 15m من Binance، gap-checked) وإعادة نفس منهجية الـ walk-forward على الداتا الأكبر، قبل ما نقرر هل نموذج الدخول نفسه ملوش edge أصلًا، ولا العينة كانت صغيرة جدًا.

## First Real Backtest (Baseline)
- 38 صفقة، Win rate 13.16%، PF 0.1865، Expectancy -0.96، Net -36.5 (100 → 52.95)
- Max Drawdown -47%
- توزيع الخروج: 11 structure_stop / 27 trailing_stop / 0 trend_break

## MFE Diagnostic (سبب رئيسي مكتشف)
- 23 من 33 صفقة خاسرة كانت وصلت MFE_R >= 0.2 (ربح ملموس) قبل ما ترجع تخسر — صفقتين وصلوا +2.85R و +1.69R وقفلوا سالب.
- السبب الجذري: `CHANDELIER_ATR_MULT` (بوحدات ATR مطلقة) مش متعاير بالنسبة لوحدة الـ R (المبنية على structure stop = ATR*0.5) → الـ trailing بيرجع يدي فايدة كتير قبل ما يقفل.

## Option 2 — Breakeven Lock (اترفض/ما اتاعتمدش)
- تفعيل عند R=1، تأخير شمعة واحدة، اختبار منعزل بدون دمج مع Option 1.
- النتيجة: تحسّن ضئيل جدًا (PF 0.1865→0.1896, Net -36.5→-36.0)، وبس 2 من 38 صفقة فعليًا قفلوا عند breakeven.
- **القرار: مش كفاية عشان يتعتمد.**

## Option 1 — R-based Trailing v4 (فيها bug — النتيجة باطلة)
- الصيغة: `active_stop_R = max(-1, peak_R*0.5)` بدون عتبة تفعيل.
- Bug: كل الـ 70 صفقة كان entry_time == exit_time (0% win rate) — لأن الصيغة سمحت للستوب يتضيق من أول شمعة دخول.
- **النتيجة دي مرفوضة/باطلة، مش دليل ضد الفكرة نفسها.**

## v5 — Gated R-trailing (بعد إصلاح الـ bug) — ما اتاعتمدش
- تفعيل بس بعد `peak_R >= 1.0`، retrace 0.5.
- النتيجة: Win rate قفز 13.16% → 35.00%، PF 0.1865 → 0.2093، **لكن Net PnL تقريبًا زي ما هو** (-36.5 → -35.9)، Max DD زي ما هو (~-47%).
- السبب: نفس الآلية اللي أنقذت صفقات خاسرة صغيرة، قصّت كمان الصفقات الرابحة الكبيرة (صفقة من R=3.72 نزلت R=0.14، وتانية من R=2.22 نزلت R=0.67) — الاتنين اتلغوا مع بعض.
- **القرار: مش تحسين حقيقي، ما اتعتمدش.**

## Test D — Pullback Depth Filter (اترفض)
- الفرضية: `pullback_depth_atr >= 0.50` AND `close_loc_pct in [0.50, 0.70)` كـ فلتر إضافي بعد الحدوث (frozen، بدون tuning).
- Full-sample subset من صفقات Baseline: 7 صفقات، PF 0.8571، Net -1.12، Win rate 28.57%.
- Walk-forward عبر 4 بلوكات زمنية: بس 1 من 4 بلوكات PF>1، وبس 1 من 4 net positive، والربح كله متركز في صفقتين بس في بلوك واحد (Block 3).
- **القرار: مرفوض كدليل edge. مفيش tuning اتعمل رد فعل على الرفض.**
- ملاحظة منهجية مهمة: أول تشغيل لـ Test D كان فيه bug — اتشغل كـ live backtest مستقل (الفلتر اتطبق جوه اللوب) بدل ما يكون subset صارم من صفقات Baseline، وده خلاه يفتح صفقة Baseline ماكانش ممكن ياخدها أصلًا. النتيجة المنفوخة (8 صفقات، PF 1.0693، net +0.6705) **اتلغت رسميًا**. النسخة المصححة بتفرض assertion إن Test D subset حقيقي من Baseline.

## خلاصة تؤثر على المرحلة الجاية
1. مفيش تعديل واحد من التجارب دي (Breakeven / R-trailing / Test D) اتحسّن بالقدر الكافي عشان يستاهل يتضاف للـ Baseline.
2. المشكلة الأساسية مش في تفاصيل الـ exit tuning — المشكلة الأعمق هي عدد الصفقات صغير (38 بس) على window زمني واحد (60 يوم)، فمش نقدر نميز بين "الاستراتيجية عندها مشكلة حقيقية" و"العينة صغيرة إحصائيًا".
3. الخطوة الصح الجاية: زيادة البيانات التاريخية الأول، مش تعديل بارامترات تانية على عينة صغيرة.
